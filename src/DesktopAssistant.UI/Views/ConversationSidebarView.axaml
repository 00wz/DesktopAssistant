<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:DesktopAssistant.UI.ViewModels"
             mc:Ignorable="d" d:DesignWidth="260" d:DesignHeight="600"
             x:Class="DesktopAssistant.UI.Views.ConversationSidebarView"
             x:DataType="vm:SidebarViewModel">

    <UserControl.Styles>
        <!--
            Переопределяем шаблон кнопок пунктов списка: убираем FluentTheme-хром
            (border, background, pressed/hover overrides). Button сохраняет логику
            кликов и IsPointerOver; всё визуальное — внутри ContentPresenter.
        -->
        <Style Selector="Button.convItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <ControlTemplate>
                    <ContentPresenter Content="{TemplateBinding Content}"
                                      Padding="{TemplateBinding Padding}"
                                      HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                </ControlTemplate>
            </Setter>
        </Style>

        <!-- Кнопки верха/низа панели (Новый чат, Настройки) -->
        <Style Selector="Button.sideBtn">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,11"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Template">
                <ControlTemplate>
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter Content="{TemplateBinding Content}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter>
        </Style>
        <Style Selector="Button.sideBtn:pointerover">
            <Setter Property="Background" Value="#3A3A3F"/>
        </Style>
        <Style Selector="Button.sideBtn:pressed">
            <Setter Property="Background" Value="#444449"/>
        </Style>

        <!-- Вращение для иконки загрузки -->
        <Style Selector="TextBlock.loadingSpin">
            <Setter Property="RenderTransformOrigin" Value="50%, 50%"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <RotateTransform/>
                </Setter.Value>
            </Setter>
            <Style.Animations>
                <Animation Duration="0:0:1" IterationCount="INFINITE">
                    <KeyFrame Cue="0%">
                        <Setter Property="RotateTransform.Angle" Value="0"/>
                    </KeyFrame>
                    <KeyFrame Cue="100%">
                        <Setter Property="RotateTransform.Angle" Value="360"/>
                    </KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>

        <!-- Пульсация для иконки ожидающего tool-вызова -->
        <Style Selector="TextBlock.questionPulse">
            <Style.Animations>
                <Animation Duration="0:0:0.8" IterationCount="INFINITE" PlaybackDirection="Alternate">
                    <KeyFrame Cue="0%">
                        <Setter Property="Opacity" Value="1.0"/>
                    </KeyFrame>
                    <KeyFrame Cue="100%">
                        <Setter Property="Opacity" Value="0.2"/>
                    </KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>
    </UserControl.Styles>

    <Grid RowDefinitions="Auto,*,Auto">

        <!-- Кнопка «Новый чат» (верх) -->
        <Button Grid.Row="0"
                Classes="sideBtn"
                Command="{Binding CreateNewChatCommand}"
                ToolTip.Tip="Начать новый диалог">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Classes="icon" Text="+" FontSize="18" VerticalAlignment="Center" Foreground="#CCCCCC"/>
                <TextBlock Text="Новый чат" FontSize="13" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>

        <!-- Список диалогов -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Нет сохранённых диалогов"
                           Foreground="#888888"
                           Margin="15,20"
                           IsVisible="{Binding !Conversations.Count}"/>

                <ItemsControl ItemsSource="{Binding Conversations}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="vm:ConversationListItemViewModel">
                            <Button Classes="convItem"
                                    Command="{Binding $parent[ItemsControl].((vm:SidebarViewModel)DataContext).OpenConversationCommand}"
                                    CommandParameter="{Binding}">
                                <Panel Background="Transparent">
                                    <!--
                                        Слои фона (снизу вверх):
                                        1. Активная сессия — тёмно-серый акцент
                                        2. Выбранный диалог — синий (перекрывает активный)
                                        3. Наведение — полупрозрачный белый оверлей поверх всего
                                    -->
                                    <Border Background="#353540"
                                            IsVisible="{Binding IsActive}"/>
                                    <Border Background="#1F3D5A"
                                            IsVisible="{Binding IsSelected}"/>
                                    <Border Background="#22FFFFFF"
                                            IsVisible="{Binding $parent[Button].IsPointerOver}"/>

                                    <!-- Контент пункта -->
                                    <Grid ColumnDefinitions="*,18" Margin="12,10">

                                        <!-- Заголовок и дата -->
                                        <StackPanel Grid.Column="0" Spacing="3">
                                            <TextBlock Text="{Binding Title}"
                                                       FontSize="13"
                                                       Foreground="White"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxLines="1"/>
                                            <TextBlock Text="{Binding FormattedDate}"
                                                       FontSize="11"
                                                       Foreground="#888888"/>
                                        </StackPanel>

                                        <!-- Иконка статуса сессии -->
                                        <Panel Grid.Column="1" VerticalAlignment="Center" Width="18">

                                            <!-- Загрузка: вращающаяся звезда; ✳ (U+2733) симметрична по всем осям -->
                                            <TextBlock Classes="icon loadingSpin"
                                                       Text="✳"
                                                       IsVisible="{Binding IsLoadingIcon}"
                                                       Classes.loadingSpin="{Binding IsLoadingIcon}"
                                                       Foreground="White"
                                                       FontSize="13"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>

                                            <!-- Пауза; ∥ (U+2225) — двойная вертикальная черта -->
                                            <TextBlock Classes="icon"
                                                       Text="∥"
                                                       IsVisible="{Binding IsPausedIcon}"
                                                       Foreground="White"
                                                       FontSize="11"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>

                                            <!-- Вопрос: ожидает одобрения tool-вызова -->
                                            <TextBlock Text="?"
                                                       IsVisible="{Binding IsQuestionIcon}"
                                                       Classes.questionPulse="{Binding IsQuestionIcon}"
                                                       Foreground="White"
                                                       FontSize="13"
                                                       FontWeight="Bold"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>

                                            <!-- Ошибка; ⚠ в Segoe UI Symbol — монохромный -->
                                            <TextBlock Classes="icon"
                                                       Text="⚠"
                                                       IsVisible="{Binding IsErrorIcon}"
                                                       Foreground="White"
                                                       FontSize="11"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                        </Panel>
                                    </Grid>
                                </Panel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопка «Настройки» (низ) -->
        <Border Grid.Row="2" BorderBrush="#3A3A3F" BorderThickness="0,1,0,0">
            <Button Classes="sideBtn"
                    Command="{Binding OpenSettingsCommand}"
                    ToolTip.Tip="Настройки">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBlock Classes="icon" Text="⚙" FontSize="14" VerticalAlignment="Center" Foreground="#AAAAAA"/>
                    <TextBlock Text="Настройки" FontSize="13" VerticalAlignment="Center" Foreground="#AAAAAA"/>
                </StackPanel>
            </Button>
        </Border>

    </Grid>
</UserControl>
